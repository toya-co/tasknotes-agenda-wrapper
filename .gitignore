# macOS / editors / agent cruft
.DS_Store
*.log
.claude/

# node (not needed — this is a no-build plugin — but ignored if you add tooling)
node_modules/

# local plugin data (runtime settings, not source)
data.json
